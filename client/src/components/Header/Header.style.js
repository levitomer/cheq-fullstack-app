import styled from 'styled-components';

export const CreateVast = styled.button`
    width: 10vh;
    height: 3vh;
    margin: 1em;
    padding: 0.25em 1em;
    border: none;
    background: #f0f8ff;
    cursor: pointer;
    border-radius: 3px;
`;

export const Container = styled.div`
    display: flex;
    position: sticky;
    justify-content: space-between;
    flex-wrap: wrap;
    align-items: center;
    width: 100%;
`;
